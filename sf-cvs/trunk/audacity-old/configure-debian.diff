--- configure.in	Mon Mar 26 22:22:19 2001
+++ configure.in	Mon Mar 26 22:28:06 2001
@@ -27,9 +27,9 @@
 	use_libmpeg3="no")
 AC_ARG_WITH(xaudio,
 	[AC_HELP_STRING([--with-xaudio],
-		[use xaudio for mp3 support [default=yes]])],
+		[use xaudio for mp3 support [default=no]])],
 	use_xaudio=$withval,
-	use_xaudio="default") dnl to differentiate between explicit and default
+	use_xaudio="no") dnl to differentiate between explicit and default
 AC_ARG_WITH(vorbis,
 	[AC_HELP_STRING([--with-vorbis],
 		[enable ogg vorbis support (libraries must be installed) [default=no]])],
@@ -57,22 +57,13 @@
 fi
 
 if [[ "$use_xaudio" = "yes" ] || [ "$use_xaudio" = "default" ]] ; then
-
-	if [[ "$host_cpu" != "i386" ]] ; then
-		AC_MSG_ERROR([*** xaudio is not supported on architectures other than i386])
-	fi
-
-	case "$host_os" in
-		"linux")
-			LIBS="$LIBS -Lxaudio/linux/lib -lxaudio"
-			;;
-		*)
-			AC_MSG_ERROR([*** xaudio is only supported on Linux])
-			;;
-	esac
-			
-	AC_DEFINE(USE_XAUDIO)
-	AC_DEFINE(MP3SUPPORT)
+	echo "*** xaudio is not included in this distribution."
+	echo "*** this tarball was specially build for Debian, and therefore all"
+	echo "*** non-free software has been removed (including xaudio) to use"
+	echo "*** xaudio, download the standard audacity tarball from sourceforge:"
+	echo "*** http://www.sourceforge.net/projects/audacity/"
+	
+	AC_MSG_ERROR([*** xaudio is not included in this distribution])
 fi
 
 dnl this complicated-looking thing is simple a nested function that checks for
